name = "vibepath"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# Environment variables — set secrets via Cloudflare dashboard:
#   Settings > Environment Variables > GEMINI_API_KEY
# For local dev: add to .env.local as GEMINI_API_KEY=your_key_here
# Get key from: https://aistudio.google.com/app/apikey (free tier covers 5,000 grounded prompts/month)
